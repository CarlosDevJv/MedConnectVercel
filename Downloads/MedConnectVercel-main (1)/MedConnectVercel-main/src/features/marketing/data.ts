/** Email institucional — ajuste conforme operações reais. */
export const CONTACT_EMAIL = 'contato@mediconnect.com.br'

export const contactMailtoHref = `mailto:${CONTACT_EMAIL}?subject=${encodeURIComponent('Agendar demonstração — MediConnect')}`
